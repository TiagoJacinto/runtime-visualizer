pre-commit:
  commands:
    lint-staged-source:
      glob: "{backend/src,browser/src}/**.{js,jsx,ts,tsx,mjs,mts,cjs,cts}"
      run: bun run lint:files -- {staged_files}

pre-push:
  commands:
    fast-checks:
      run: bun run prepush
