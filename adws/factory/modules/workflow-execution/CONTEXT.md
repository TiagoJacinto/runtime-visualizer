# Workflow execution context

A Workflow is typed control flow over bounded phases. A primitive invocation produces evidence; an artifact is a typed handoff reference. The source repository is never the worker workspace. The Factory does not merge, push, deploy, or integrate automatically.
